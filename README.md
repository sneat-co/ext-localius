# ext-localius

Public contract repository for Localius. `frontend/` is the sole publisher of
`@sneat/extension-localius-contract`; the paired `localius` repository owns the
runtime package and standalone application.
